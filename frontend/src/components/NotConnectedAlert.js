import {Alert} from "react-bootstrap";
import React from "react";

function NotConnectedAlert() {
    return <Alert variant="danger">Lost connection to the server</Alert>;
}

export default NotConnectedAlert;